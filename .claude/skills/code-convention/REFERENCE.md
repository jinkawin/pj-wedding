# Project Structure — Next.js App Router

## Folder Layout

```
├── .gitlab/            # GitLab MR templates
├── k8s/                # K8s deployment & service manifests per env
├── public/             # Static assets (fonts, images)
├── src/
│   ├── app/            # App Router — pages, layouts, route handlers
│   ├── assets/         # Styles, fonts, icons
│   ├── components/     # Reusable React components
│   ├── configs/        # Project configuration files
│   └── hooks/          # Shared custom hooks
```

## Top-level Files

| File | Purpose |
|------|---------|
| `.dockerignore` | Files excluded from Docker build context |
| `.env` | Default environment variables |
| `.env.local` | Local-only env vars (gitignored) |
| `.env.development` | Env vars for `next dev` |
| `.env.production` | Env vars for `next build` |
| `.gitignore` | Files excluded from Git |
| `.gitlab-ci.yml` | GitLab CI/CD pipeline config |
| `.prettierrc` | Prettier formatting config |
| `docker-compose.yml` | Docker Compose service definitions |
| `Dockerfile` | Docker image build script |
| `next.config.js` | Next.js configuration |
| `package.json` | Dependencies and scripts |
| `tailwind.config.ts` | Tailwind CSS configuration |
| `tsconfig.json` | TypeScript compiler options |

## Auto-generated (do not commit)

| Folder | Generated by |
|--------|--------------|
| `.next/` | `next dev` / `next build` |
| `dist/` | Build output |
| `node_modules/` | `yarn install` |
